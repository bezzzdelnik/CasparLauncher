<ui:DialogWindow x:Class="CasparLauncher.WebServerSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CasparLauncher"
        xmlns:l="clr-namespace:CasparLauncher.Properties"
        xmlns:ui="clr-namespace:BaseUISupport.Controls;assembly=BaseUISupport"
        xmlns:converters="clr-namespace:BaseUISupport.Converters;assembly=BaseUISupport"
        xmlns:helpers="clr-namespace:BaseUISupport.Helpers;assembly=BaseUISupport"
        mc:Ignorable="d"
        MinWidth="500"
        Width="600"
        MinHeight="300"
        Height="350"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Foreground="{DynamicResource Foreground}"
        Title="{helpers:Localization WebServerSettingsWindow_Title}">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="boolToVisibility" False="Collapsed" True="Visible" />
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.IsSharedSizeScope="True">
            <GroupBox Header="{helpers:Localization WebServerSettings_Parameters}" Margin="0 0 0 10">
                <StackPanel>
                    <ui:ConfigItem Label="{helpers:Localization WebServerSettings_Port}" Type="Integer" Binding="{Binding Source={x:Static local:App.Launchpad}, Path=WebServerPort, Mode=TwoWay}"/>
                    <ui:ConfigItem Label="{helpers:Localization WebServerSettings_AllowedAddresses}" Type="Text" Binding="{Binding Source={x:Static local:App.Launchpad}, Path=WebServerAllowedAddresses, Mode=TwoWay}"
                                   Description="{helpers:Localization WebServerSettings_AllowedAddressesDescription}"/>
                    <ui:ConfigItem Type="CheckBox" Label="{helpers:Localization WebServerSettings_AutoStart}" Binding="{Binding Source={x:Static local:App.Launchpad}, Path=WebServerAutoStart, Mode=TwoWay}" 
                                   Description="{helpers:Localization WebServerSettings_AutoStartDescription}"/>
                </StackPanel>
            </GroupBox>
            <GroupBox Header="{helpers:Localization WebServerSettings_Status}">
                <StackPanel>
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <TextBlock Text="{helpers:Localization WebServerSettings_Status}"/>
                        <TextBlock Text=": "/>
                        <TextBlock Text="{Binding Source={x:Static local:App.Launchpad}, Path=WebServerStatusText}"/>
                    </StackPanel>
                    <TextBlock Margin="5" TextWrapping="Wrap">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="URL: {0}">
                                <Binding Source="{x:Static local:App.Launchpad}" Path="WebServerUrl"/>
                            </MultiBinding>
                        </TextBlock.Text>
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontFamily" Value="Consolas"/>
                                <Setter Property="Foreground" Value="{DynamicResource HighlightAccent}"/>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <StackPanel Margin="5">
                        <TextBlock Text="{helpers:Localization WebServerSettings_ApiEndpoints}" FontSize="10" Foreground="{DynamicResource ForegroundDisabled}"/>
                        <TextBlock Text="{helpers:Localization WebServerSettings_ApiGetExecutables}" FontSize="10" Foreground="{DynamicResource ForegroundDisabled}"/>
                        <TextBlock Text="{helpers:Localization WebServerSettings_ApiStart}" FontSize="10" Foreground="{DynamicResource ForegroundDisabled}"/>
                        <TextBlock Text="{helpers:Localization WebServerSettings_ApiStop}" FontSize="10" Foreground="{DynamicResource ForegroundDisabled}"/>
                        <TextBlock Text="{helpers:Localization WebServerSettings_ApiRestart}" FontSize="10" Foreground="{DynamicResource ForegroundDisabled}"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="{helpers:Localization WebServerSettings_Close}" Click="CloseButton_Click" Margin="5 0" Padding="10 5"/>
        </StackPanel>
    </Grid>
</ui:DialogWindow>

